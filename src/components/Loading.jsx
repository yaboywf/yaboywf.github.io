import "../styles/loading.scss";

const Loading = () => {
    return (
        <main className="loading">
            <div></div>
            <p>Loading...</p>
        </main>
    );
}

export default Loading;